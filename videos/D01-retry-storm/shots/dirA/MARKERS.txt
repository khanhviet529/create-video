Shot dirA — required event markers (6)

Each row needs ONE element in the composition carrying that id, which becomes
visible (opacity >= 0.5) at the given time. The element can be any visual you
like — the moving object, a tick, a label, a ring. Layout is your call.

  #ev-r1_retry  @  1.28s   clients spawn  — each of the 20 failures is retried three times: 60 retries
  #ev-r1_load   @ 2.561s   clients write  — next round arrives at 180 — and nobody asked for more
  #ev-r2_retry  @ 6.402s   clients spawn  — 240 retries
  #ev-r2_load   @ 7.683s   clients write  — 360
  #ev-r3_retry  @ 11.707s   clients spawn  — 780 retries
  #ev-r3_load   @ 13.537s   clients write  — 900 — seven and a half times the baseline
