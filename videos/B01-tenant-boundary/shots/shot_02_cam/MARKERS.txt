Shot shot_02_cam — required event markers (3)

Each row needs ONE element in the composition carrying that id, which becomes
visible (opacity >= 0.5) at the given time. The element can be any visual you
like — the moving object, a tick, a label, a ring. Layout is your call.

  #ev-authn    @ 3.036s   api check  — token verified — this really is an acme user
  #ev-lookup   @ 7.022s   api access  — SELECT by primary key, no tenant predicate
  #ev-respond  @ 10.628s   api access  — another tenant's record returned to the caller

Notes:
  - camera "global_to_root_cause": every event marker must stay inside the frame, except #ev-authn
